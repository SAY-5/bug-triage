# Provider selection: fake | anthropic | openai
PROVIDER=fake

# Use deterministic hash-based embedder (1) or sentence-transformers (0)
HASH_EMBEDDER=1

# Database
DATABASE_URL=postgresql+psycopg://bug_triage:bug_triage@localhost:5432/bug_triage

# BYOK (only when PROVIDER!=fake)
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Logging
LOG_LEVEL=INFO
